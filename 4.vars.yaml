- name: play level variables
  hosts: local
  connection: local
  become: yes
  vars:                                       #these are all play level variables
    doing: writing ansible palybook
    level: Intermediate level
    experience: "4years"

  tasks:
  - name: print variables
    ansibile.builtin.debug:
      msg: "Doing is : {{vars}}; experience is : {{exerience}}"